'''
Main script that holds commands.
'''

import discord
import config # in the same directory ; imports bot_prefix and token
import asyncio

client = Bot(command_preefix=config.start['bot_prefix']

@client.command(name='birthday',
                description = '',)
